<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center">

        <Button
            android:id="@+id/mobile_games_button"
            android:layout_width="270dp"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_button"
            android:text="@string/mobile_games"
            android:textAllCaps="false"
            android:textSize="20sp"
            android:layout_marginBottom="40dp"
            android:layout_marginTop="50dp"/>

        <Button
            android:id="@+id/card_games_button"
            android:layout_width="270dp"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_button"
            android:text="@string/card_games"
            android:textAllCaps="false"
            android:textSize="20sp"
            android:layout_marginBottom="40dp"/>

        <Button
            android:id="@+id/lvl_games_button"
            android:layout_width="270dp"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_button"
            android:textAllCaps="false"
            android:text="@string/lvl_games"
            android:textSize="20sp"
            android:layout_marginBottom="40dp"/>

        <Button
            android:id="@+id/set_reminder_button"
            android:layout_width="150dp"
            android:layout_height="wrap_content"
            android:background="@drawable/rounded_button"
            android:textAllCaps="false"
            android:text="@string/set_alarm"
            android:textSize="16sp"/>

    </LinearLayout>

    <ImageView
        android:id="@+id/card_games_nextlvl_logo_imageview"
        android:layout_width="350dp"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/nextlvl_logo" />

</androidx.constraintlayout.widget.ConstraintLayout>